import {
  GET_ALL_WALLET,
  SET_USER,
  GET_WALLET,
} from './query';

import {
  CREATE_WALLET,
} from './mutation';

export const Query = {
  GET_ALL_WALLET,
  SET_USER,
  GET_WALLET,
};

export const Mutation = {
  CREATE_WALLET,
};
