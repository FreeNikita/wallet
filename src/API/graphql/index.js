import {
  GET_ALL_WALLET,
  SET_USER,
} from './query';

import {
  CREATE_WALLET,
} from './mutation';

export const Query = {
  GET_ALL_WALLET,
  SET_USER,
};

export const Mutation = {
  CREATE_WALLET,
};
